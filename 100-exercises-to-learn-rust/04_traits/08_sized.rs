// 🔑 要点：动态大小类型（DST）在编译时大小未知
// str 是 DST，不能直接获取其大小
// 编译器会在编译时报错：`size_of::<str>()` 是不允许的
// 我们总是通过引用 &str 来使用 str（引用有固定大小）

pub fn example() {
    // 注释掉下面这行即可通过编译
    // std::mem::size_of::<str>();
}
